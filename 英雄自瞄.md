







## 26.1.25

现在发给电控的yaw角是rpy_的yaw角，但在我的理解里，这个rpy_不一定是面向整车中心的yaw角

明天可以试试，==是不是第一次识别到的rpy_是多少度，接着就定那个角度了==

我现在的思路是，只要一旋转起来，就把那个算等待板的yaw角换成整车中心算出来的yaw角

然后我想把线速度加到整车中心上

大符，看昨天那个视频，那个**选板**其实是没用上的，所以加了个TEMP_LOST的状态，但是还没测过

那个2转1的阈值记得调调，调的高可能无法击打到第二块装甲板，调的低没啥用

## 26.1.22

今天把命中率的视频拍出来，然后准备调符（当时没拍出来）



## 26.1.21

改成同济选板加定头后，击打旋转🎯达到了81.25%的命中率，但是打移动🎯出现了问题，原因是判断条件是火控为1,所以导致发送的角度有时是0,有时是yaw角，导致跟不上。

改选板改这么久，也该浅浅总结下了

- 首先发过去的yaw角如果是转速高的（>2），就用等待板的思想，我判定**所选的装甲板的朝向角跟当前陀螺仪的角的差值**是否小于一定阈值，然后我接着要用陀螺仪的yaw角去计算等待板，然后去转动另一个算出来的yaw角，通过这个总结，我仔细一想，我所选的最优装甲板是基于上一时刻的，但是下一时刻又是一个新的yaw角，不一定是最适合的
- 转速不高的，直接打那个板就好了，不用等待板

刚刚做梦想到的，就是打静止的靶就直接瞄和直接打所预测出来的装甲板就可以了，但是，==打旋转的和旋转移动的==，要用center_yaw角去打，这样 想法做废

仔细一想，还是用target_armor的yaw角，

## 26.1.20

可能选板有些问题，我看代码里写的选板是用装甲板相对**odom坐标系**的偏航角，去减当前的云台yaw角，这种情况确实会导致头不断抖动，我以前还一直以为是滤波器的问题，有点sb了

今天改改选板，然后准备调符

### 弹道解算

我看到英雄代码是只解算前哨站，分两种情况来计算：

1. 角速度绝对值大于1的话，会根据旋转中心计算一个**假想板**

   1. 获取子弹打到装甲板的时间+从图像发布到我开始进行弹道解算的问题+偏置时间。

   2. 用角速度（固定的，官方文档有给）预测当前位置经历上面时间后，前哨站三个装甲板的位置，并计算三个装甲板离车辆的位置和离假想板的位置。

   3. 计算**假想板**位置的均值。

   4. 用离车辆距离最小的装甲板和假想板去弹道解算出yaw和pitch角，

   5. ```c++
                  Eigen::Matrix2d R_odom2center,R_odom2armor;
                  R_odom2center << std::cos(alpha), std::sin(alpha),
                      -std::sin(alpha), std::cos(alpha);
                  R_odom2armor << std::cos(beta), std::sin(beta),
                      -std::sin(beta), std::cos(beta);
               
                  Eigen::Matrix2d R_center2armor = R_odom2center.transpose() * R_odom2armor;
                  double decision_angle = -std::asin(R_center2armor(0, 1));
      //计算出装甲板相对假想板的相对角度差
      ```

   6. 执行火控`gimbal_msg.fire_advice = std::fabs(decision_angle) * 180.0 / M_PI <= 9.0 ? true : false; `.

2. 角速度绝对值小于1的话

   1. 就直接对yaw角进行预测，直接选当前装甲板就好了。
   2. 但是火控方面变化角度阈值减小了`bool yaw_flag = std::fabs(gimbal_msg.yaw_diff) <= 2.0 ? true : false;bool pitch_flag = std::fabs(gimbal_msg.pitch_diff) <= 2.0 ? true : false;`
   3. 执行火控

```c++
#include<iostream>
#include<cmath>
using namespace std;

int main()
{
    int n;
    cin>>n;
    uint32_t temp = n;
    int count1 = 0;
    int count0 = 0;
    for(int i = 0;i < 32;i++)
    {
        if(temp&1)
        {
            count1++;
        }
        else
        {
            count0++;
        }
        temp<<=2;
    }
    if(count1 > count0)
    {
        cout<<count1<<",1 Win"<<endl;
    }
    else
    {
        cout<<count0<<",0 Win"<<endl;
    }
    return 0;
}
```

### 更改的弹道解算方法和飞行时间的计算

新的弹道解算方法耗时比
PE比原来的解算方法耗时多了0.2ms

后续Yancey称，这种其实影响不算大，重要的是框架原因



### 画辅助吊射

我觉得应该是autoaim_odom的坐标系下

1. - [x] 学习marker怎么使用 
2. - [x] 把转化出来的点去算出pitch角
3. - [x] 知道飞行时间和pitch角，算出实际打到的位置，并画出实际打到的位置
4. 把yaw和pitch发给电控

还没写弹道的代码

整体代码运行流程，先启动串口节点，发布两个坐标关系（我突然发现，电控好像不用给我发值也可以，但是那样可以显示出gimbal_link），接着启动tracker节点，接收雷达的数据，并进行转换，丢进去弹道解算，把解算出来的飞行时间和pitch角留下，然后把三维位置算出来，并画出来。yaw，pitch，distance那些要发给电控，
