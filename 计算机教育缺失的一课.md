### 第1讲

#### 几个符号

- 一个是这个<输入流，一个是输出流>
- | 他的作用是左边程序的输出作为右边程序的输入

以上两个命令，输入输出的重定向是程序所不知道的，也就是说，如果有`sudo echo 500 >brightness`这条指令，500只作为输入传入bringhtness文件，==但是他前面的超级用户是没有传输的==，导致会没权限修改文件bringhtness。

还有一个比较推荐的指令就是tee指令

`echo 100 | sudo tee 1.txt`，这个指令会把100输入1.txt,而且还会把这个输入发送到终端输出。

### 第2讲


